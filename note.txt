--step 1 create file -------
create docker-compose.yml

--step 2 put command---------------

docker run -d \
  --name=mysql-workbench \
  --cap-add=IPC_LOCK \
  -e PUID=1000 \
  -e PGID=1000 \
  -e TZ=Asia/Bangkok \
  -p 3000:3000 \
  -p 3001:3001 \
  -v ~/mysql-workbench/config:/config \
  --shm-size="1gb" \
  --restart unless-stopped \
  lscr.io/linuxserver/mysql-workbench:latest

-- step3 check container---

docker ps | grep mysql-workbench
```

### ขั้นตอนที่ 5: เข้าใช้งาน MySQL Workbench
เปิดเว็บเบราว์เซอร์และเข้าไปที่:
```
https://localhost:3001
```

หรือถ้าอยู่คนละเครื่อง:
```
https://your-server-ip:3001

-- step6 connect database---

เมื่อเปิด MySQL Workbench แล้ว:

คลิก "+" เพื่อสร้าง connection ใหม่
กรอกข้อมูล:

Connection Name: ตั้งชื่อที่ต้องการ
Hostname: IP ของ MySQL Server
Port: 3306 (default)
Username: root หรือ user ที่มี
Password: (ถ้ามี)



คำสั่งเพิ่มเติมที่อาจใช้:
ดู logs:
bashdocker logs mysql-workbench
หยุด container:
bashdocker stop mysql-workbench
เริ่ม container อีกครั้ง:
bashdocker start mysql-workbench
ลบ container:
bashdocker stop mysql-workbench
docker rm mysql-workbench



======================

check tutorial


https://www.youtube.com/playlist?list=PLZPZq0r_RZOMskz6MdsMOgxzheIyjo-BZ


---------

not working 
just do up on youtube only

password :1111


===========
check process

part 1
MySQL: How to create a DATABASE

// create database
CREATE DATABASE myDB;


// switch to use database 
USE mydb

//drop database
DROP DATABASE mydb


//read database only can not modify
ALTER DATABASE mydb READ ONLY = 1;


//read database only can modify
ALTER DATABASE mydb READ ONLY = 0;

---------------

part2

//create table
CREATE TABLE employees (
	employee_id INT,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    hourly_pay DECIMAL(5,2),
    hire_date DATE
);

//choose table
SELECT * FROM employees;


//rename table employees to workers
RENAME TABLE employees TO workers;

//rename table workers to employees;
RENAME TABLE workers TO employees;

//drop(delete) table
DROP TABLE employees;


//modify add field phone_number in employees table
ALTER TABLE employees
ADD phone_number VARCHAR(15);


//rename field phone_number to email in employees table
ALTER TABLE employees
RENAME COLUMN phone_number TO email;


// modify colum email have VARCHAR(100)  in employees table
ALTER TABLE employees
MODIFY COLUMN email VARCHAR(100);



//modify colum email show after colum last name  in employees table
ALTER TABLE employees
MODIFY COLUMN email VARCHAR(100)
AFTER last_name;

SELECT * FROM employees;



//modify colum email to show first in employees TABLE
ALTER TABLE employees
MODIFY COLUMN email VARCHAR(100)
FIRST;

SELECT * FROM employees;


//modify to drop colum email in employees TABLE
ALTER TABLE employees 
DROP column email;

SELECT * FROM employees;